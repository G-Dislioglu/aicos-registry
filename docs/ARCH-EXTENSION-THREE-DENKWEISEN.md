# Architecture Extension: Three Denkweisen

> Historical aliases / phantom IDs may appear. Resolve through `docs/ARCHITECTURE_CARD_CROSSWALK.md` before machine use.
>
> provenance: provisional_from_chat

## Status

This file was created from the chat-governed documentation integration flow.
The full original body is not fully recoverable in the current working context, so this document is intentionally marked `provisional_from_chat` rather than treated as a registry-truth source.

## Intended Role

This architecture note is preserved as part of the repository's human architecture layer.
Its purpose is to hold a conceptual extension document describing three complementary ways of thinking (`Denkweisen`) without changing live registry truth.

## Governance Boundary

Interpret this document as an architectural narrative reference only.
When the text names cards, principles, or solution proofs:

- resolve exact IDs against `cards/**`
- prefer `index/INDEX.json` for discovery
- use `index/ALIASES.json` only for explicit backward-compatibility mappings present in the repo
- use `docs/ARCHITECTURE_CARD_CROSSWALK.md` for historical or ambiguous references

## Recovery Note

If the complete source text is reintroduced later, replace this file body in-place while preserving the historical-alias warning at the top.
