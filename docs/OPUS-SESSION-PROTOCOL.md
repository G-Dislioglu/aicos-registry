# OPUS Session Protocol

> Historical aliases / phantom IDs may appear. Resolve through `docs/ARCHITECTURE_CARD_CROSSWALK.md` before machine use.
>
> provenance: provisional_from_chat

## Status

This file was created from the chat-governed documentation integration flow.
The full original body is not fully recoverable in the current working context, so this document is intentionally marked `provisional_from_chat` rather than treated as a registry-truth source.

## Intended Role

This architecture note is preserved as part of the documentation reference layer, not as a canonical runtime or card-definition source.
Use it to retain session-protocol intent, operator framing, and design language around OPUS-style working sessions.

## Governance Boundary

For machine resolution, automation, and ID truth:

- Use `cards/**` as the canonical card source.
- Use `index/INDEX.json` as the generated scan surface.
- Use `index/ALIASES.json` only for the narrow historical alias mappings that actually exist in this repo.
- Use `docs/ARCHITECTURE_CARD_CROSSWALK.md` when this document references legacy names, shorthand labels, historical aliases, or phantom IDs.

## Recovery Note

If the complete source text is reintroduced later, replace this file body in-place while preserving the historical-alias warning at the top.
